anthropic>=0.40.0
groq
fastapi
uvicorn[standard]
pydantic>=2
python-dotenv
python-multipart
httpx
requests
qdrant-client>=1.11.0
voyageai
pdfplumber
pdf2image
docling[mlx]
openpyxl
pandas
markdown
yt-dlp
youtube-transcript-api
# openai-whisper  # optional — heavy dependency, uncomment if needed
tiktoken
numpy
# PyTorch: trên Linux mặc định pip sẽ kéo bản CUDA (auto dùng GPU NVIDIA nếu có).
# Trên macOS mặc định có MPS (GPU Apple Silicon).
# CPU fallback khi không có GPU. Override: RERANKER_DEVICE=cpu/cuda/mps
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.11.0+cu126
sentence-transformers
pytest-asyncio
boto3